services:
  # Backend Service
  - type: web
    name: ink-well-backend
    runtime: node
    rootDir: backend
    buildCommand: npm install
    startCommand: node server.js
    envVars:
      - key: PORT
        value: 10000
      - key: MONGODB_URI
        sync: false # Set this in the Render dashboard
      - key: JWT_SECRET
        generateValue: true

  # Frontend Service
  - type: web
    name: ink-well-frontend
    rootDir: frontend
    buildCommand: npm install && npm run build
    staticPublishDir: dist
    envVars:
      - key: VITE_API_URL
        fromService:
          type: web
          name: ink-well-backend
          property: host
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
